version: "3.9"
services:
  render-service:
    build: .
    ports:
      - "${PORT:-8001}:${PORT:-8001}"
    env_file:
      - .env
    environment:
      - REMOTION_DIR=/app/remotion
    restart: unless-stopped
